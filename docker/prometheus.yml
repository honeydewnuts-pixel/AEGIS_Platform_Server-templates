global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: 'aegis-api'
    static_configs:
      - targets: ['aegis-api:8000']
    metrics_path: /metrics
